# Required for the persistent visitor counter (/api/visits.js).
# Create a free database at https://console.upstash.com/ and paste its
# REST credentials below. Never commit the real values — only this
# placeholder file with empty/example values belongs in git.
# In Vercel: Project Settings → Environment Variables → add both keys.

UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
